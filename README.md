# PenuX-AP-Severity API (deploy branch)

Minimal deploy mirror of `PenuX-AP-Severity/` from the main branch —
contains only what's needed to run the FastAPI service on Render:
`api/`, `src/penux_ap/`, the small data files the `/models/*` endpoints
read, and packaging/build files.

Source of truth is `PenuX-AP-Severity/` on `claude/pensive-pascal-a0l7a8` —
this branch is regenerated from there, not edited directly.

RESEARCH USE ONLY. Not validated for clinical use.
